# g-heal-claw SDK 接入所需的公开 DSN
# 本地联调时指向 apps/server（默认 3001）
NEXT_PUBLIC_GHC_DSN=http://publicKey@localhost:3001/demo

# 环境标签，写入每个事件
NEXT_PUBLIC_GHC_ENV=development

# 发布版本号，便于按版本过滤
NEXT_PUBLIC_GHC_RELEASE=0.0.1
